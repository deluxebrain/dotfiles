# zshenv
# used for ZDOTDIR only
# all other exports should be in .zshrc

# to use in alternative to /etc/zshenv
# - remove ZDOTDIR export from /etc/zshenv
# - move this file to ~/.zshenv

export ZDOTDIR="$HOME/.config/zsh"
