# Custom ZSH aliases

# allow ASDF to use python3 as global version
alias python="python3"

# alias watch to support aliases ( note the trailing _ )
alias watch="watch "

# kubernetes and friends
alias mk="minikube"
alias i="istioctl"

# get my public ip
alias myip="curl -s https://ifconfig.me; echo"
